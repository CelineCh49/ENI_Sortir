<form id="desistementForm" action="{{ path('sortie_desistement', {'id': sortie.id}) }}" method="post">
    <input type="hidden" name="_csrf_token" value="{{ csrf_token('desistement_sortie') }}">
<button type="submit" class="default_secondary_button text-yellow-600 hover:text-yellow-800">Se désister</button>
</form>